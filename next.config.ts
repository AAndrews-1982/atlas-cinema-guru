// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  // Add your Next.js configuration options here
  // For example, enable future Next.js features, set up redirects, rewrites, etc.
  typescript: {
    ignoreBuildErrors: true,
  },
};

module.exports = nextConfig;
